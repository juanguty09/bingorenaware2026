# Production — server and frontend share the same Railway domain,
# so all API/socket calls use relative URLs automatically.
# Only set these if you ever host the frontend separately.
VITE_API_URL=https://your-app.up.railway.app
VITE_SOCKET_URL=https://your-app.up.railway.app
